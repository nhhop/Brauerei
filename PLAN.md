# Brauerei — Backlog & offene Punkte

Heimbrauerei-Steuerung auf ESP32-Basis: Library `SensActCtrl` (Sensor-/Aktor-/Regler-Primitive + Remote-Transporte) plus Web-UI-Consumer `BrewControl` (Firmware + Preact-SPA).

**Dieses Dokument enthält nur Offenes** — Bugs, bekannte Einschränkungen, Backlog und Gedanken zu neuen Features. Sobald ein Punkt umgesetzt ist, wandert er ersatzlos hier raus; die erledigte Historie steht chronologisch in [SESSION.md](SESSION.md) / [SESSION-archive.md](SESSION-archive.md). Architektur- und API-Referenz stehen in den READMEs ([SensActCtrl/README.md](SensActCtrl/README.md), [BrewControl/README.md](BrewControl/README.md)).

SensActCtrl und BrewControl werden gemeinsam geplant, unabhängig davon, welches Teilprojekt eine Änderung betrifft.

Jeder Punkt trägt am Ende eine Einschätzung *(Modell · Aufwand · Planmodus)*: **Modell** — Sonnet für klar umrissene Änderungen, Opus für Architektur-Entscheidungen und mehrdeutige/cross-cutting Aufgaben. **Aufwand** — klein (eine Datei/Klasse, unter einer Session), mittel (mehrere Dateien/Komponenten, eine Session), groß (mehrere Sessions, neue Architektur). **Plan** — ob vor der Umsetzung ein Plan (`EnterPlanMode`) sinnvoll ist, weil Scope oder Architektur noch Entscheidungen offen lässt; „ohne Plan" heißt direkt umsetzbar. Eigene Einschätzung, keine Garantie — bei Bedarf gemeinsam anpassen.

---

## Bugs & bekannte Einschränkungen

- **preact-routers Klick-Delegation ließ am Gerät einen Link durchrutschen** (2026-09-10) — `href="/"` aus der Seitenleiste wurde vom delegierten Click-Listener auf `document` nicht genommen, der Browser navigierte echt und startete die SPA neu (Beleg und Analyse in SESSION.md). Lokal nicht reproduzierbar: derselbe Link wird abgefangen, `exec('/', '/', {})` trifft. Die Seitenleiste routet inzwischen selbst und ist damit unabhängig davon, **alle übrigen Links der App hängen aber weiter an der Delegation** — `SettingsCard`, `Breadcrumb`, `AlertCenter`, `LogsPage`. Die funktionierten im Test, dieselbe Ursache könnte sie aber genauso treffen. Wenn irgendwo ein unerklärter Neustart der SPA auffällt: hier zuerst suchen. Sauber wäre, das Routing einmal zentral zu lösen statt pro Komponente. *(Sonnet · mittel · mit Plan — betrifft alle Link-Stellen)*
- **ESP-NOW verwirft Pakete >250 Byte silently.** `EspNowTransport::sendDataPacket_()` gibt bei Überschreitung `false` zurück, kein Retry. Betrifft v.a. Controller mit vielen Params (DualStage/SplitRangePID) — deren Meta-Publish kann dauerhaft scheitern. `lastErrorMessage()` zeigt seit 2026-08-29 „Paket zu groß (…)", aber nichts liest das aktiv aus/loggt es. *(Sonnet · mittel · mit Plan — Scope-Frage: nur loggen oder auch über API/UI surfacen)*
- **`WebhookService::getOrCreate()` cached strikt nach `(port, peerUrl)`.** Zwei verschiedene Peers, die sich denselben lokalen Port teilen wollen, erzeugen zwei separate `WebServer`-Instanzen auf demselben physischen Port → Bind-Konflikt. Im Test 2026-08-29 umgangen (anderer Port für den zweiten Consumer), Cache-Design selbst nicht gefixt. *(Sonnet · mittel · mit Plan — Design-Entscheidung wie der Konflikt aufgelöst wird)*
- **WebSocket-Hub ohne Authentifizierung und TLS** (2026-09-16) — der Hub-Port (`WebSocketTransport` als Server, Einstellungen → Konnektivität → WebSocket) nimmt jede Verbindung aus dem LAN an und liegt außerhalb des Zugriffsschutzes; ein beliebiger Client kann dem Hub Werte für Remote-Items unterschieben. `wss://` wird abgelehnt (kein TLS). Gleiches Grundproblem wie beim Webhook-Server. Naheliegend: Shared Secret im Handshake (Header oder Query-Parameter), das der Hub prüft. *(Opus · mittel · mit Plan — Schlüsselverteilung und UI, ggf. gemeinsam mit Webhook lösen)*
- **LittleFS-Boards (esp32dev/lolin_s2_mini): kein Boot-Flash-Recovery, keine Log-Retention** (2026-08-28) — `FirmwareUpdater::flashFromSdImage()` passt nicht in die 256-KB-Datenpartition (Netzwerk-OTA unberührt); `LogStore` rotiert dort nicht, sollte nicht unbegrenzt loggen. *(Opus · groß · mit Plan — zwei Teilprobleme, berührt Partitionslayout)*
- **Ablaufsteuerung blieb einmal stehen — vermutlich stand der loopTask** (beobachtet 2026-09-12 am LilyGo S3, einmalig, nach einem Reboot wieder normal). Ein laufendes Programm schaltete nicht weiter: Schritt „Einmaischen" (60 s, mit Freigabe) stand nach 3021 s immer noch auf `running`, weder Schrittwechsel noch `awaiting`. **Belege:** Die Firmware selbst lieferte `stepRemainingSec: 0`, ihre Uhr war also gültig und die Haltezeit abgelaufen; `GET /api/programs` antwortete sofort, und das nimmt denselben rekursiven Mutex wie `tick()` — der Runner hing also **nicht** in seiner eigenen Sperre. Bleibt: `ProgramRunner::tick()` wurde gar nicht mehr aufgerufen, weil der loopTask davor stand (`loop()`: `registry.tick()` → `webUI.tick()` → `logs_.tick()` → `programs_.tick()`). Die Web-API läuft auf dem AsyncTCP-Task und antwortet weiter, während der loopTask hängt — das Gerät wirkt dadurch gesund. **Nicht mehr geprüft** (Board war schon neu gestartet): ob die Sensorwerte eingefroren waren. **Nächstes Mal zuerst, vor dem Reboot:** zweimal `GET /api/snapshot` im Abstand von ein paar Sekunden und `sensors[].state.t` (millis der letzten Messung) vergleichen — steht der Wert, hängt der loopTask und die Programmlogik ist nicht die Ursache; dazu `GET /api/programs` sichern und, wenn USB dranhängt, den seriellen Output. **Verdächtige in dieser Reihenfolge:** SD-gebundene Arbeit vor dem Programm-Tick (`logs_.tick()` schrieb alle 5 s, kurz zuvor lief ein UI-Upload samt `swapAssets_()`); der frisch angelegte `IDS1`-Aktor, den `mash` seitdem treibt — **dieser Verdächtige ist seit 2026-09-23 ausgeräumt**, der Bit-Bang ist raus und `registry.tick()` liegt bei 3,8 ms statt 141 ms; als Blocker in dieser Größenordnung scheidet er aus (die Messungen dazu legten dafür einen neuen Verdächtigen frei, siehe Datei-Download-Punkt); und die Pin-Überschneidung in der aktuellen Config — „Riptide Pumpe" (DigitalOutput) und `IDS1` (`pin_white`) liegen beide auf GPIO 7. Kein Zusammenhang zur Multi-Regler-Umstellung erkennbar: dieselbe Firmware lief davor und danach korrekt durch alle Schritte inklusive Freigabe. *(Opus · mittel · mit Plan — Root Cause unklar, erst eingrenzen)*
- **`pio device monitor` auf TinyUSB-CDC unter Windows instabil** (ESP32-S2 und -S3): Monitor verliert sporadisch Output. Workaround: PowerShell-Skript mit `System.IO.Ports.SerialPort`, DTR/RTS-Toggle für Reset, `ReadExisting()`-Loop (siehe `BrewControl/README.md`). Mögliche Permanent-Fixes: `--filter direct`, anderes Terminal (PuTTY/Tera Term), Monitor-Reconnect-Tuning. *(Sonnet · klein · ohne Plan — Workaround existiert schon, nur Komfort)*
- **AutoTunePID-Dependency zeigt auf ungetaggten `main`-Commit** (`SensActCtrl/library.json`, Commit `34c6f39`, Stand 2026-07-26) — `v1.1.6` bleibt die einzige veröffentlichte Version von `lily-osp/AutoTunePID` und enthält den alten, physikalisch bedeutungslosen Relay-Timer (siehe SESSION.md 2026-09-05). Arduino-IDE-Library-Manager-Nutzer (`library.properties`, nur Namensliste ohne Version) bekämen bei einer Standalone-Installation weiterhin `v1.1.6`. Beobachten, ob/wann `lily-osp/AutoTunePID` einen neuen Tag veröffentlicht, dann auf den Tag umstellen. *(Sonnet · klein · ohne Plan — nur Pin-Update, sobald ein Tag existiert)*

- **Bearbeiten-Hinweis verkürzt die Layout-Fläche um 50 px** (2026-09-19, beim Fix der starren Kartenbereiche gemessen) — das Hinweisfeld unter den Tabs steht im Fluss der Spalte mit `lg:h-full`, der Layout-Bereich ist im Bearbeiten-Modus dadurch 801 statt 851 px hoch (dazu 18 px Polsterung/Rahmen je Bereich). Kartenbereiche trifft das seit dem Fix nicht mehr (Inhaltshöhe), flexible Bereiche skalieren beim Moduswechsel aber weiterhin um diese 50 px. Sauber wäre ein Hinweis, der die Fläche nicht verkürzt (z. B. schwebend statt im Fluss) — Platzierung ist eine Designfrage, Text ist bewusst ausführlich. *(Sonnet · klein · ohne Plan)*
- **`POST /api/estop` ist bewusst unauthentifiziert** (2026-09-20, beim Latch-Umbau geklärt) — der Handler ruft kein `requireAuth`, damit ein Not-Aus auch aus gesperrter UI auslösbar bleibt; `DELETE /api/estop` (Freigabe) ist dagegen gegated. Das ist so gewollt und in `openapi.yaml` jetzt so dokumentiert. Offen bleibt die Frage, ob derselbe Gedanke für weitere „abschaltende“ Routen gelten soll (z.B. einzelne Aktoren auf `enabled:false` setzen) — heute brauchen die Auth. *(Sonnet · klein · mit Plan — Sicherheitsabgrenzung, keine reine Umsetzung)*
- **Datalog-Verlauf springt beim Kalibrieren** (2026-09-21) — die Kalibrierung wirkt nur auf neue Messwerte, bereits aufgezeichnete Punkte bleiben unkalibriert; im Chart entsteht an der Kalibrierstelle ein Sprung. Der Kalibrier-Dialog weist darauf hin. Denkbar: Kalibrierzeitpunkt als Marker im Verlauf (oder in der Log-Session vermerken), damit der Sprung als solcher erkennbar ist. *(Sonnet · klein · mit Plan — Marker-Darstellung und Ablage klären)*
- **Neue Rechner-Seite (`/rechner`): einige Formelkonstanten noch unverifiziert** (2026-09-19, Refraktometer/ABV/Endvergärungsgrad am 2026-09-19 nachgebessert) — `BrewControl/web/src/brewMath.ts` trägt noch `// TODO(verify)`-Marker an: Einmaischtemperatur-Konstante 0,41 (Palmer, metrische Variante), die drei Effizienz-Checkpoints (Maischeeffizienz/Sudhausausbeute/Brewhouse-Efficiency nutzen dieselbe Formel an unterschiedlichen Messpunkten — ob das exakt den jeweils üblichen deutschen/englischen Konventionen entspricht, ungeprüft), Karbonisierung (Fahrenheit-basiertes RestCO₂-Polynom, DME-Multiplikator 1,4 gegenüber Traubenzucker). Spindel-Korrektur (kubisches Polynom) bleibt mit hoher Zuversicht ungeprüft. **Refraktometer-Korrektur, ABV und Endvergärungsgrad sind seit 2026-09-19 gegen eine Primärquelle verifiziert** (siehe SESSION.md) — der Marker und die vorherige, falsche Formel (Koeffizienten 0.1808/0.8192 direkt auf den Brix-Wert angewendet, mit der Balling-Formel verwechselt) sind raus; ebenso `gravityUnits.ts`s Plato⇄SG-Konvertierung, die jetzt exakt invertierbar ist statt zwei unabhängig gefitteten Näherungen. Restliche Formeln vor Vertrauen gegen eine Primärquelle prüfen. *(Sonnet · klein · ohne Plan — pro Formel eine Quelle finden und den Marker entfernen)*
- **Datei-Downloads blockieren den `loop()` sekundenlang** (gemessen 2026-09-23 am esp32dev) — bei parallelen `GET /api/files/download` auf das 124-KB-UI-Bundle steigen **alle** Abschnittstimer gleichzeitig: `registry.tick()` 188 ms, `webUI.tick()` 193 ms, der Rest-Block 414 ms, einmal sogar **14,2 s** in einem einzigen Durchlauf. Das ist nicht der IDS-Keep-Alive — die Messung lief mit dem RMT-Pfad, der im Leerlauf 3,8 ms braucht. Verdächtig ist der LittleFS-Lesepfad im loopTask bzw. die Aushungerung durch AsyncTCP (Priorität 10, ohne Core-Bindung). Dazu passt, dass das Board bei 3 SSE + 6 parallelen Downloads auf **beiden** Ports ~40 s lang keine Verbindung mehr annahm (lwIP-Socket-Limit, kein Absturz — Uptime lief durch, Ping antwortete). Vermutlich derselbe Mechanismus wie beim loopTask-Hänger oben. *(Opus · mittel · mit Plan — Root Cause unklar, erst eingrenzen)*
- **Display: Wischen blockiert `loop()` bis ~140 ms** (gemessen 2026-09-24 am LilyGo, siehe SESSION.md) — LVGL läuft aus `loop()`; während einer Wischbewegung rendert es pro Frame den ganzen Schirm in Software (Ringe mit Kantenglättung, 64-px-Font), `displayTick` bis 132 ms, `loop()`-p99 72 ms. Im Ruhebetrieb p99 22 ms. Heute harmlos (IDS-Keep-Alive per RMT, Regler im Sekundentakt, SSE 1 Hz), vom Nutzer so akzeptiert. Falls es stört: eigener LVGL-Task auf Core 0 (Snapshot + Command-Queue) oder DMA-Double-Buffer; zuerst messen, ob Rendern oder Blit dominiert (Werkzeug: Branch `spike/lvgl-metrics`). *(Opus · mittel · mit Plan)*
- **Display liest `Registry::label()` ohne Sperre** (2026-09-24) — `labels_` ist eine `std::map`, die der AsyncTCP-Task beim Umbenennen beschreibt, während der loopTask für die sichtbare Seite liest. Seltenes Fenster (nur beim Umbenennen), aber undefiniertes Verhalten; dieselbe Klasse wie das ungesperrte Löschen von Items (`DynamicItems::remove*`) gegen `registry.tick()`. Sauber: Label-Zugriffe in der Library sperren oder Umbenennen in den loopTask verschieben. *(Opus · mittel · mit Plan — betrifft die Library)*
- **`Arduino_CO5300.cpp` trägt einen GPL-3.0-Header** (2026-09-24) — die Datei stammt aus LilyGos Fork (`BrewControl/firmware/vendor/Arduino_GFX-1.3.7`), deren Repo sonst MIT ist; upstream Arduino_GFX hat eine eigene Lizenz. Relevant erst bei Weitergabe der AMOLED-Firmware. Nutzer-Entscheidung: später ersetzen, z. B. durch den CO5300-Treiber aus einer neueren upstream-Version, zurückportiert auf den 1.3.7-Stand. *(Sonnet · mittel · mit Plan)*
- **Die Platte meldet keinen Fehler, wenn der Topf fehlt** (`IdsInductionCooker`, 2026-09-24) — am Gerät geprüft: Topf bei 30 % und bei 100 % heruntergenommen, über 240 bzw. 85 Sekunden blieb `fault` **null** und der Fehlercode 0. Die Zuordnung „Code 2 = kein Topf“ in `errorMessages` stammt aus der Tabelle der Library, nicht aus einer Beobachtung an dieser Platte — sie ist damit unbelegt, und mit ihr die Annahme, ein Topf-Test sei ein verfügbarer Auslöser für den Fehlerpfad. Praktische Folge: es gibt derzeit **keinen Weg, einen IDS-Fehler auf Zuruf zu erzeugen**, was jede künftige Arbeit am Fehlerpfad auf Gelegenheitsfunde angewiesen macht. *(Sonnet · klein · ohne Plan — Beobachtung, kein Fix)*

## Hardware-Verifikation offen

- **Safe-Area-Ränder im Vollbild: Edge offen** — unter **Chrome am Gerät bestätigt** (2026-09-12), der schwarze Streifen oben ist weg. Unter **Edge steht er noch.** Offen ist, ob Edge den Cutout überhaupt freigibt: Die Entscheidung fällt nicht in Blink, sondern im Android-Activity-Flag `layoutInDisplayCutoutMode` — Chrome setzt es auf `SHORT_EDGES`, sobald eine Vollbild-Seite `viewport-fit=cover` deklariert, und Edge bringt seine eigene Android-Hülle mit. Tut Edge das nicht, bleibt `safe-area-inset-top` dort 0 und kein CSS erreicht den Streifen. Billiger Test: ist der Streifen pures Schwarz, liegt er außerhalb des Fensters; hat er das Grau der Seite, greift unser Padding nicht. Sicherer Test per `edge://inspect` über USB: in Vollbild `getComputedStyle(document.documentElement).getPropertyValue('--safe-t')` lesen — `0px` heißt Browser-Limit, dann ersatzlos abhaken. Firefox nutzte schon vorher den ganzen Schirm. *(Sonnet · klein · ohne Plan)*
- **iOS: Home-Screen-Start prüfen** (offen seit 2026-09-10) — Manifest und `apple-*`-Meta-Tags sind drin und lokal geprüft, Android ist am Gerät durch (Adressleiste bleibt dort systembedingt, der Vollbild-Schalter deckt es ab). Am iPhone fehlt der Test: „Zum Home-Bildschirm", Start über das Icon → Fenster ohne Adressleiste; dabei mitprüfen, ob der FAB unten rechts über dem Home-Indicator liegt und ob SSE (`/api/events`) im Home-Screen-Fenster weiterläuft. Der Vollbild-Schalter erscheint dort bewusst nicht, `document.fullscreenEnabled` ist auf dem iPhone `false`. *(Sonnet · klein · ohne Plan)*
- **Sensor-Kalibrierung: HX711-Tara und YF-S201-Faktor am echten Sensor** (2026-09-21) — der Kalibrier-Assistent ist am esp32dev mit DAC→ADC-Schleife (`AnalogInput`) durchgespielt (Zwei-Punkt, Zurücksetzen, Bearbeiten, Reboot); die Migration der Altkeys `scale`/`calibration` ist per API belegt (Config wird umgeschrieben, Gain stimmt). Nicht geprüft, weil keine Hardware: Tara-Knopf an einer echten Wägezelle (Offset auf den Live-Rohwert, danach Faktor mit Referenzgewicht) und Volumen-Faktor am echten YF-S201 (bekannte Menge durchlaufen lassen → Gain). Ebenso ein Temperatursensor (DS18B20/PT100) im Eiswasser mit Ein-Punkt-Offset. *(Sonnet · klein · ohne Plan — reine Verifikation)*
- **SSR-Heizung unter Last mit Oszilloskop** — TPO-Schaltflanken prüfen. *(Sonnet · klein · ohne Plan)*
- **Gärsteuerung Dual-Output-Regler E2E am Gerät** — DualStage/SplitRangePID anlegen, beide Ausgänge live, Anti-Short-Cycle der Kühlstufe, Kühl-Aktor-Löschen-Block. *(Sonnet · mittel · ohne Plan — mehrere Szenarien, aber reine Verifikation)*
- **Alarme: `fault()`- und AutoTune-Trigger am Gerät** (offen seit 2026-09-05) — Schwellwerte (raise/clear/Hysterese/enable-Toggle, tote Referenz), Programm-Ende und `awaiting` sowie die gesamte UI wurden am LilyGo verifiziert; diese beiden Trigger nicht. Der `fault()`-Pfad ließ sich dort nicht auslösen, ohne MQTT vom eingebetteten Broker auf einen toten externen Host umzustellen (zu invasiv für einen Testlauf); AutoTune hätte bis zum `done`-Ereignis durchlaufen müssen. Beim nächsten passenden Anlass mitnehmen: Sensor mit totem MQTT-Broker → `fault`-Alert und nach Wiederkehr `cleared`; AutoTune an einem PID bis zum Ende → `autotune`-Alert. Ebenfalls noch ungeprüft: Verhalten vor dem NTP-Sync (`ts: 0` → „Zeit unbekannt“). *(Sonnet · klein · ohne Plan)*
- **Sensorgetriggerte Schritte E2E am Gerät** (offen seit 2026-09-10) — `end:"sensor"`-Schritt (`Condition` `{ref, op, value, hyst}`) im Browser gegen einen Demo-Sensor verifiziert; am echten Gärlauf noch nicht: Schritt mit „Gravity < 1.010" anlegen, Istwert live in der ProgramCard, automatisches Weiterschalten beim Unterschreiten, Hysterese-Latch, Kombination mit „Freigabe abwarten", Reboot mitten im Sensor-Schritt (Resume + saubere Neubewertung). *(Sonnet · klein · ohne Plan)*
- **PID-AutoTune E2E am Gerät** — Status idle→running→done, übernommene Gains, Abbruch, 3-Phasen-Stepper + %-Balken korrekt; insbesondere prüfen, dass ein echter Kessel/Maischebottich jetzt spürbar länger als die alten ~10 s braucht (Beleg, dass der Relay-Timing-Fix vom 2026-09-05 wirkt und nicht nur die UI-Zahlen kosmetisch sind). Ebenso **SplitRangePID-AutoTune E2E**. *(Sonnet · klein · ohne Plan)*
- **Anzeigename (Label) E2E am Gerät** (2026-09-23) — native Tests, Firmware-Compile, OpenAPI-Lint und Frontend-Typecheck/-Build grün, aber ohne Hardware in dieser Session nicht geprüft: Label an einem regler-verdrahteten Sensor setzen/ändern (kein 405 „referenced by a controller" mehr), Label übersteht einen Reboot, ID-Änderung am selben Sensor liefert die neue verständliche Fehlermeldung, Karten/Dropdowns zeigen das Label statt der ID mit ID-Tooltip. *(Sonnet · klein · ohne Plan — reine Verifikation)*
- **Regelbereich (`range_min`/`range_max`) E2E am Gerät** (2026-09-12) — Library-Tests grün, Firmware kompiliert, Frontend gegen den `pnpm dev`-Mock geprüft (Farblogik, Klick-Edit, Drag, Sensor-Fallback); der Dev-Mock kennt das neue Feld nicht, also am echten Gerät noch nicht geprüft: `range_min`/`range_max` im `AddItemModal` setzen, `POST /api/controllers` persistiert sie, Snapshot liefert `rangeMin`/`rangeMax` zurück, Regler-Slider im Dashboard nutzt sie als Skala statt des Sensor-Fallbacks. *(Sonnet · klein · ohne Plan — reine Verifikation)*
- **Öffnen des COM-Ports über CP210x (esp32dev) resettet das Board** (2026-09-17, bestätigt für COM6/esp32dev) — bekanntes Verhalten für LilyGo/COM9 (siehe Test-Boards-Memory) tritt auch beim esp32dev-Board auf, selbst mit `DtrEnable=false`/`RtsEnable=false` nach `Open()`. Während eines HTTP-getriebenen E2E-Tests führte ein kurzer Serial-Read-Versuch zu einem unerwarteten Reboot und temporär leeren/verzögerten API-Antworten. Für HTTP-/API-Tests an diesem Board Serial-Zugriff vollständig vermeiden. *(kein Fix nötig — Dokumentationshinweis)*
- **WebSocket-Transport: Rest-E2E am Gerät** (offen seit 2026-09-16) — mit esp32dev (Hub) und S2 Mini (Leaf) verifiziert (siehe SESSION.md). Offen: (1) **zwei Leaves gleichzeitig** am Hub — beide liefern Werte, ein `/set` an Leaf A wirkt nicht auf Leaf B (LilyGo war belegt, esp32dev diente als Hub); (2) **die neue UI am Gerät** — Einstellungsseite „WebSocket" (Hub-Status „Verbundene Geräte", Publish-Status, Speichern), die Gruppe „Boards im Netz" mit Koppeln-Button samt Passwort-Abfrage und der WebSocket-Button im Remote-Dialog. Der komplette API-Pfad dahinter ist seit 2026-09-18 am Gerät verifiziert (siehe SESSION.md), die UI selbst bisher nur gegen einen HTTP-Mock — es fehlt also nur das Durchklicken im Browser gegen ein echtes Board; (3) beim Test mit unerreichbarem Hub lief eine von ~700 Snapshot-Abfragen am S2 in einen 3-s-Timeout — beobachten, ob das wiederkehrt. *(Sonnet · klein · ohne Plan)*
- **Gruppenkarte für Multi-Channel-Sensoren am Gerät** (2026-09-20) — im Browser gegen einen lokalen Mock verifiziert (siehe SESSION.md), am echten Board noch nicht: HCSR04/YF-S201 als Basis-Ref aufs Dashboard legen, Zeilen-Modus umschalten und **nach einem Reboot** prüfen, dass die Kanalschlüssel in `sensorModes` (`tank.distance`) in `/config/dashboards.json` stehen und die Karte gleich wieder aufgeht; dabei mitnehmen, wie das neu gepackte Raster (gemessene Zeilen-Spans) auf einem echten Dashboard mit Charts und Programmen aussieht. *(Sonnet · klein · ohne Plan — reine Verifikation)*
- **GY-521/MPU-6050-Tiltsensor: Achsenwahl und Vorzeichen am echten Board** (2026-09-23) — `GY521TiltSensor` (SensActCtrl) nutzt für den Komplementärfilter eine feste, iSpindel-typische Annahme (`atan2(-ax, sqrt(ay²+az²))` für den Beschleunigungs-Winkel, GyroX für die Integration); native Tests decken nur die Filter-Numerik und die Kanalform ab (Stub-Werte), nicht die reale Einbaulage. Am echten Gerät prüfen: welche Achsenkombination bei der tatsächlichen Schwimmkörper-Montage den erwarteten Winkel liefert (ggf. Achsen/Vorzeichen tauschen), danach `poly`-Kalibrierung des `angle`-Kanals gegen reale SG-Messpunkte wie beim iSpindel-Vorbild. Wire-Pin-Kollision auf dem AMOLED-Board (siehe eigener Punkt oben) betrifft diesen Sensor genauso. *(Sonnet · klein · ohne Plan — reine Verifikation, ggf. Formel-Anpassung im Code danach)*
- **Dashboard-Layout: Drag per echtem Touch am Tablet** (2026-09-20) — die Persistenz-E2E am Gerät ist durch (siehe SESSION.md), ebenso der Escape-Abbruch; ein Drag mit echtem Finger fehlt. Am Desktop lässt sich nur eine synthetische Touch-Geste erzeugen (`PointerEvent` mit `pointerType:'touch'`, committet korrekt, `touch-action: none` am Griff gesetzt) — ob der Browser am Tablet die Geste nicht doch als Scroll/Pan abgreift, beweist das nicht. Am Tablet prüfen: Griff im Bearbeiten-Modus greifen, Karte ziehen, andocken, Seite scrollt dabei nicht mit. *(Sonnet · klein · ohne Plan — reine Verifikation)*
- **`ReloadRetry`-Komponente (Preact): Auto-Reconnect-Erfolgsfall nicht E2E geprüft** (2026-09-18) — Countdown/Fallback-UI wurde gegen ein echtes Testboard geladen, die reboot-auslösenden Aktionen selbst (WLAN-Wechsel, Hostname-Wechsel, Settings-Speichern, Firmware-Update, Backup-Restore) wurden dabei bewusst nicht angeklickt, um das laufende physische Gerät nicht neu zu starten. Offen: nach einer dieser Aktionen tatsächlich abwarten, ob der periodische `fetch(url,{mode:'no-cors'})`-Probe nach dem echten Reboot greift und die Seite automatisch neu lädt bzw. bei `NetworkPage`s Hostname-/WLAN-Wechsel auf `{host}.local` umleitet. *(Sonnet · klein · ohne Plan — reine Verifikation)*

---

## Backlog

Grob nach Bereitschaft / Aufwand; Abhängigkeiten stehen inline. Jeder Punkt bekommt bei Bedarf eine eigene Spec → Plan → Implementierung.

- **Remote-Discovery für Webhook** — `DiscoveryScanner`/`RemotePublisher` sind transport-agnostisch; offen ist nur die Firmware-Anbindung (`RemoteDiscovery`, `/api/remote/discover?transport=`) und die Frage, an welche Peers die Anfrage überhaupt geht (Webhook hat keinen Broadcast-Begriff, jedes Item hat seine eigene `peer_url`). *(Sonnet · klein · ohne Plan)*
- **„Zuletzt gesehen" pro Remote-Item** (2026-09-18) — ein Socket-Status pro Peer gibt es nur in der Client-Rolle; der Hub kennt lediglich `clientCount()`. Ein Zeitstempel des letzten empfangenen Werts pro Remote-Item wäre für alle Transporte gleichwertig und für die Diagnose besser: ein totes Board ist heute von einem stehenden Messwert nicht zu unterscheiden. *(Sonnet · mittel · mit Plan — Snapshot-Shape betroffen)*
- **ESP-NOW: Verschlüsselung + Unicast für `/tune`** — Befehle gehen seit 2026-09-16 unicast (`EspNowPeerTable`), damit ist PMK/LMK pro Peer möglich (Heizungs-Aktoren sind heute für jeden in Funkreichweite schaltbar). `/tune` bleibt Broadcast, weil der Eltern-Topic `controller/<id>` nie gesendet wird. *(Opus · mittel · mit Plan — Schlüsselverteilung klären)*
- **Gradienten/Ableitungen (Library)** — rate-of-change (°C/min, K/min, L/min²). **Offene Designfrage:** (a) zusätzlicher Suffix-Kanal am Sensor (`MLT.rate`, Basiskanal behält die nackte ID) oder (b) eigenes abgeleitetes Item, das einen Quellkanal liest und einen neuen liefert (`MLT_gradient`; funktioniert auch für HX711/Remote-Werte, kein ID-Umbau). Problem bei (a): ein DS18B20 hat genau einen Kanal mit leerem Key, auf dessen nackte ID Controller, Alarme, Logs, Dashboards und Publisher zeigen — ein zweiter Kanal macht ihn zum Mehrkanal-Sensor. Tendenz (b), da ein Gradient Auswertung ist und nicht Hardware. *(Opus · groß · mit Plan)*
- **Datalog — API-seitige Dezimierung** (LTTB/Douglas-Peucker mit `?points=`) für sehr lange Archiv-Zeiträume; Live-Chart-Append an `intervalSec` angleichen (aktuell 1 Hz). Vorab prüfen, ob das Log auch ohne tatsächlichen Messpunkt an der Cursor-Position einen Wert anzeigen kann (Interpolation) — wenn ja, lässt sich die Punktzahl massiv reduzieren (client- und serverseitig): neuer Wert ersetzt den vorherigen, solange die Differenz unter einem Schwellwert bleibt (nur Zeitstempel aktualisieren), erst bei Überschreiten wird ein neuer Punkt angelegt. *(Opus · groß · mit Plan — Algorithmus-Genauigkeit + API + Frontend)*
- **Mehrsprachigkeit** — Sprache auf der „Zeit & Region"-Seite wählbar (Umbenennung von „Zeit & Formate"). *(Opus · groß · mit Plan — i18n-Grundgerüst betrifft das ganze Frontend)*
- **Hardware-RTC (PCF8563)** als Fallback für Betrieb ohne WiFi — LilyGo T-Display-S3-AMOLED hat den Chip onboard; einmalige NTP-Sync schreibt in die RTC, danach zeitstempelstabil auch offline. *(Sonnet · mittel · mit Plan)*
- **AP-Modus** als wählbare Alternative (Standalone ohne Router) — verschoben, weil ohne Internet kein NTP (bricht Datalog-Timestamps); sinnvoll zusammen mit Hardware-RTC. Ebenso statische IP / DHCP-Konfiguration. *(Sonnet · mittel · mit Plan — mehrere Netzwerk-Zustände sauber durchdenken)*
- **Countdown-Programm: Gesamtzeit statt Einzel-Dauer je Schritt vorgeben**; Anzeige als Abstand zum Start/Ende statt reiner Dauer. *(Sonnet · mittel · mit Plan — Datenmodell-Änderung am Programm-Schema)*
- **Neue Icons für Website (Favicon/Manifest) und Push-Benachrichtigungen.** *(Sonnet · klein · ohne Plan)*
- **Zeiteinheit für Intervalle beidseitig wählbar** (z. B. „xxx min / xxx min"), vordere Einheit muss ≤ hintere sein. *(Sonnet · klein · ohne Plan)*
- **Migrations-Logik für alte Programme entfernen**, sobald nicht mehr gebraucht. *(Sonnet · klein · ohne Plan)*
- **Charts sollen nur im Dashboard leben** — Vorgabe/Entscheidung festhalten, nicht auf anderen Seiten duplizieren. *(Sonnet · klein · ohne Plan)*
- **Prüfen: eigene Menü-Seite für Logs** (statt/zusätzlich zur aktuellen Einbindung). *(Sonnet · klein · mit Plan — Navigations-Entscheidung)*
- **Alternative Slider-Darstellung für Aktoren mit diskreten Stufen** (statt stufenlosem Slider). *(Sonnet · mittel · mit Plan — UX für diskrete Aktoren)*
- **Programm-Komponente: anderen Button als „Start" je nach Zustand hervorheben** (Highlight). *(Sonnet · klein · ohne Plan)*
- **Alternative MQTT-Broker prüfen** (z. B. picomqtt) als Ersatz/Ergänzung für den eingebetteten Broker. *(Sonnet · klein · mit Plan — erst Recherche/Vergleich, dann Entscheidung)*
- **Manueller Sensor** (2026-09-22) — virtueller Sensor ohne Hardware, dessen Wert über die GUI eingetragen wird (z. B. Extraktgehalt aus manueller Spindelmessung, Titration) und danach wie jeder andere Sensorwert geloggt/dargestellt wird. Offene Fragen: eigener `Sensor`, dessen `tick()` nichts tut und dessen Wert per neuer API-Route gesetzt wird (`POST /api/sensors/{id}/value` o. ä.) statt aus Hardware zu lesen; Verhalten zwischen Einträgen (letzter Wert hält vs. als „veraltet" markiert, ggf. mit Ablaufzeit); UI-Eingabefeld im Dashboard oder eigene Seite. *(Sonnet · mittel · mit Plan — neuer Sensor-Typ ohne tick-Quelle, API-Oberfläche)*
- **Werkseinstellungen: Grundkonfiguration statt harter Defaults** (2026-09-22) — „Zurücksetzen" soll auf eine mitgelieferte Config-Datei zurückfallen statt auf Code-Defaults, damit sich die Werkseinstellung pro Gerät/Deployment anpassen lässt (z. B. Boards mit fester Sensor-Bestückung). Offene Fragen: wo die Datei liegt (SD-Karte parallel zu den Nutzer-Configs, oder ins Firmware-Image eingebettet/PROGMEM), was beim Reset genau ersetzt wird (nur `config.json` oder auch Dashboards/Programme), und ob sie beim Flashen mitgeliefert wird oder separat kopiert werden muss. *(Sonnet · klein-mittel · mit Plan — Ablage-Ort und Scope des Resets klären)*

## Größere Brocken (eigene Spec vor Umsetzung)

- **Peripherie-Abstraktion** — `Peripheral`-Interface (`id`, `type`, `begin`/`tick`/`end`) + `PeripheralRegistry`, das geteilte Busse (OneWire / I2C / SPI / CAN) beim ersten passenden Consumer automatisch anlegt. Sensoren/Aktoren referenzieren per Bus-Id oder hängen sich anhand der Pins automatisch an. Verallgemeinert die bestehende `getOrCreateBus`-Logik in `DynamicItems.cpp`, beseitigt die SPI-Pin-Duplizierung bei MAX31865. Port-Expander / CAN-Transceiver = spätere konkrete Peripherie auf derselben Naht (Hardware aktuell nicht vorhanden). **Gedanke 2026-09-22:** auch Geräte, die *Fähigkeiten nachrüsten*, gehören hierher — externer DAC (MCP4725/I²C) für Boards ohne DAC (S3), Port-Expander (zusätzliche Digital-Pins), 433-MHz-Funkmodul, RS485-Transceiver. Ein Peripheriegerät würde dann Capabilities „anbieten" (z. B. `dac`, `gpio`), und Aktor-/Sensor-Typen werden nur angeboten, wenn Board oder Peripherie die Fähigkeit liefern; UI: eigener Peripherie-Manager (Geräte anlegen, Bus/Adresse, Status). Reines Rückgrat, kein User-Feature für sich — enabler für den Pin-Manager. *(Opus · groß · mit Plan)*
- **Pin-Manager** (firmware, auf Peripherie-Abstraktion aufbauend) — Board-Capability-Map (per Board-Define): Input-only Pins 34–39, Strapping-Pins, Flash/PSRAM-belegte Pins (z.B. 33–37 auf S3-AMOLED), DAC-Pins 25/26 (S3: gar keine — Capability-Map ist damit auch die Quelle für das Ausblenden von DAC-Aktoren, siehe Unterpunkt), Default I2C/SPI/UART; Pins von Port-Expandern erscheinen als virtuelle Pins derselben Map. `GET /api/pins` liefert frei/belegt; Belegung = Peripherie (geteilt/beitrittsfähig) + Items mit exklusiven Pins. Stufen: Tier 1 Belegung + Map → Tier 2 Constraint-Query (interrupt-/serial-/dac-fähig) → Tier 3 Protokoll-Vorschläge (bestehende Bus-Peripherie bevorzugen). *(Opus · groß · mit Plan — setzt Peripherie-Abstraktion voraus)*
  - **Anlass: nichts prüft die Belegung** — die UI lässt denselben GPIO mehrfach vergeben, und die Board-Defaults sind nicht mit bereits vergebenen Pins abgeglichen (`lilygo`-Env setzt `-DBREWCTL_ONEWIRE_PIN=1`, `-DBREWCTL_SSR_PIN=2`). Am LilyGo hat das binnen drei Tagen **zweimal** zugeschlagen: erst GPIO 1 (OneWire-Bus + `IDS1.pin_white`) und GPIO 2 (DS18B20 + `Agitator`), nach deren Bereinigung dann GPIO 3 — diesmal `IDS1.pin_white` **und** `agitator`, dazu ein **Strapping-Pin**. Unbemerkt blieb dabei `Riptide Pumpe` auf **GPIO 4**, dem Batteriespannungs-ADC des Boards. Am 2026-09-25 von Hand aufgelöst (pin_white → 9, agitator → 8, Pumpe → 2; 3 und 4 sind jetzt frei), die Ursache bleibt. Die Map bräuchte neben „frei/belegt“ also auch „technisch möglich, aber keine gute Idee“: Strapping-Pins, der Batterie-ADC, USB (19/20), UART0 (43/44), die RGB-LED (48). Freie, unbedenkliche GPIOs auf diesem Board sind heute nur noch **47** und die drei gerade vergebenen.
  - **Nicht nur Pins sind knapp: RMT-Kanäle auch** (2026-09-25) — `IdsCooker` belegt pro Platte einen sendefähigen RMT-Kanal, und davon gibt es board-abhängig nur **8** (ESP32), **4** (ESP32-S2) bzw. **4** (ESP32-S3, von 8 Kanälen senden nur 0–3). Heute nutzt nichts sonst im Projekt RMT. Kritisch ist das Verhalten bei Erschöpfung: `rmtInit()` liefert `NULL`, und `Init()` fällt stillschweigend auf den Software-Pfad zurück, der den Aufrufer ~139 ms je Frame blockiert — die überzählige Platte schlägt also nicht fehl, sie macht den RMT-Umbau für alle rückgängig und zieht den `loop()` herunter. Dieselbe Klasse wie die Pin-Belegung: eine Hardware-Ressource, die die UI beliebig oft vergeben lässt. Zwei Stufen denkbar: (a) die Library meldet den Fallback nach oben statt ihn zu verschlucken, `IdsActuator::fault()` macht ihn sichtbar; (b) die Capability-Map zählt RMT-TX-Kanäle wie Pins und lehnt das Anlegen ab. Seit 2026-09-25 im Code am Fallback kommentiert.
  - **DAC-Aktor wird auch am ESP32-S3 angeboten** (2026-09-22, Nutzer-Entscheidung 2026-09-25: zusammen mit Pin-Manager/Peripherie umsetzen statt vorab als Einzelfix) — der Original-ESP32 hat DAC auf GPIO 25/26, der S2 auf 17/18, der S3 (LilyGo T-Display-S3-AMOLED) gar keinen. Die UI listet den DAC-Ausgang unabhängig vom Board, ein Anlegen auf dem S3 kann nicht funktionieren. Lösung über die Capability-Map (`dac`), die das Board oder ein externer DAC (z. B. MCP4725 über I²C) als Peripheriegerät liefert; `AddItemModal` bietet den Typ nur an, wenn die Fähigkeit vorhanden ist.
- **Interaktives Display: nächste Ausbaustufen** (firmware, LilyGo T-Display-S3-AMOLED-1.75) — Stufe 1 ist seit 2026-09-24 da (siehe SESSION.md, `BrewControl/README.md` → „Rundes Touch-Display“): Items eines Dashboards als Wischseiten, Dashboard-Wechsel per Hoch/Runter, Bedienung von Reglern und Aktoren. Offen:
  - **Layout spiegeln** — langfristiges Ziel (Nutzer-Entscheidung 2026-09-22) bleibt, dass das Display die Dashboard-Konfiguration spiegelt. Das `layout`-Feld in `DashboardStore` ist für die Firmware ein opaker Web-Grid-Baum; eine Abbildung aufs runde 466×466-Panel braucht eine eigene Spec. Heute wird nur die Reihenfolge Regler → Sensoren → Aktoren genutzt.
  - **Charts, Programme, Timer** als eigene Seiten (Dashboard-Listen `charts`/`programs`/`timers` werden heute ignoriert).
  - **Fremdgesteuerte Items** — das Display sperrt sie; die Web-UI bietet stattdessen „schalten und Regler deaktivieren / Programm pausieren“. Ob das Display das auch soll, ist offen.
  - **Firmware je nach Display laden** — passende Firmware-Variante online oder von SD-Karte laden (baut auf dem OTA-Varianten-Modell auf).
  - *(Opus · groß · mit Plan)*
- **Arduino Core 3 / ESP-IDF 5 + Curier statt esp-webPush** — `ESPToolKit/esp-webPush` ist seit 2026-08-03 archiviert; wir fahren auf seinem letzten Commit plus `esp_webpush_patch.py` (Arduino Core 2.x nennt `esp_crt_bundle_attach` in seiner eigenen Header-Kopie `arduino_esp_crt_bundle_attach` und überdeckt damit den echten IDF-Header). Der gepflegte Nachfolger [ZekStack/curier](https://github.com/ZekStack/curier) ist mit dieser Platform **nicht baubar**: er nutzt `std::span`, das libstdc++ erst ab GCC 10 mitbringt, und `espressif32@6.10.0` pinnt für Arduino fest GCC 8.4 (`-std=gnu++20` kennt der Compiler nicht einmal dem Namen nach, nur `gnu++2a`; damit wiederum verliert er bei `static constexpr`-Membern die implizite Inline-Eigenschaft → Linker-Fehler). Der Wechsel hängt also am Platform-Sprung auf Arduino Core 3 / ESP-IDF 5 — Breaking Changes in LEDC, WiFi-Events u.a., plus Neu-Verifikation aller drei Boards. Curier bringt zusätzlich `Strata` als weitere Abhängigkeit mit. Lohnt sich erst, wenn der Core-Sprung ohnehin ansteht — **LVGL ist dafür kein Argument mehr**: LVGL 8.4 baut unter GCC 8.4/`gnu++17` problemlos, und LilyGos Arduino_GFX-1.3.7 ebenso (Spike 2026-09-22). **Umgekehrt beim Sprung beachten:** das vendorte `BrewControl/firmware/vendor/Arduino_GFX-1.3.7` stammt aus der Zeit vor Core 3 und ist darauf ungeprüft. Bis dahin kostet „keine Commits mehr" wenig: Web Push ist ein eingefrorener RFC-Stack (8291/8292), und `PushService` kapselt die Lib vollständig. *(Opus · groß · mit Plan — Platform-Migration, nicht das Push-Feature)*
- **HTTPS-Support** — nur noch Transport-Härtung für Auth und API; Push läuft seit 2026-09-09 ohne (siehe SESSION.md). Zwei Korrekturen aus der Recherche 2026-09-05: Self-signed trägt für Push nicht (Chrome verweigert SW-Registrierung bei Zert-Fehlern, Durchklicken hilft nicht), und `esp32async/ESPAsyncWebServer`+`AsyncTCP` haben weiterhin keinen Server-TLS-Pfad — bliebe Migration auf `esp_https_server` (1300+ Zeilen `WebUI.cpp`, ~50 Routen, SSE, zwei Upload-Pfade). Varianten: Reverse-Proxy im Heimnetz (billig, aber untauglich für Fremdgeräte) · eigener ACME-Dienst nach `*.plex.direct`-Vorbild (für Endnutzer null Aufwand, größte Betreiber-Verpflichtung) · Chrome Local Network Access (https-SPA darf nach Permission-Prompt Klartext an private IPs/`.local`, aber Chrome-only + CORS). Achtung: SSE über TLS verdoppelt grob den RAM-Footprint pro Client. Für Tests ohne echtes Zertifikat: Android Secure-Flag beachten sowie `chrome://flags/#unsafely-treat-insecure-origin-as-secure` bzw. `edge://flags/#unsafely-treat-insecure-origin-as-secure` als Workaround. Erwägen, HTTPS-Support per Compiler-Flag ein-/ausbaubar zu machen (spart Flash/RAM, wenn nicht gebraucht). *(Opus · groß · mit Plan — Zert-Strategie ist eine Architektur-Entscheidung)*
- **Autodiscovery für Remote-Transporte** (BrewControl, vorgemerkt 2026-09-16) — Geräte sollen sich im LAN finden, statt Hub-URL, Broker oder Peer-URL von Hand einzutragen (WebSocket-Hub, MQTT-Broker inkl. eingebautem Broker, Webhook). Rollen: der Dienst kündigt an, der Leaf sucht und wählt per Name — mehrere Boards können gleichzeitig Hub sein, Discovery liefert nur die Adresse (löst nebenbei wechselnde DHCP-IPs). Mechanismus offen: **mDNS-SD** (`_sensactctrl._tcp` + TXT-Records im bereits laufenden mDNS; gesucht wird nach dem Diensttyp, Gerätenamen müssen nicht vorher bekannt sein; Arduino-`queryService` blockiert → asynchrone ESP-IDF-Query-API nötig; Ergebnisse nur aus dem Suchfenster) vs. **UDP-Broadcast** (eigenes Format, passiv lauschend, Liste immer aktuell). Der Ort beeinflusst die Wahl: mDNS läuft bisher nur in der BrewControl-Firmware (`main.cpp`: `MDNS.begin` + `_http._tcp`), SensActCtrl enthält keinen mDNS-Code — soll Discovery auch für eigenständige SensActCtrl-Knoten gelten, gehört sie in die Library, und dort ist UDP eigenständiger, weil `MDNS.begin(hostname)` Sache der Anwendung ist. Abgrenzung: Die Item-Suche im Remote-Dialog („Geräte suchen", `sensactctrl/discover`, seit 2026-09-16) findet nur Items auf einem bereits verbundenen Transport, keine Adressen — für Webhook fehlt sie noch (siehe Backlog). **Für WebSocket ist beides seit 2026-09-18 erledigt** (mDNS `_sensactctrl._tcp` + `POST /api/remote/pair`, siehe SESSION.md); offen sind hier also nur noch MQTT-Broker und Webhook-Peer. Dabei ist zu klären, ob das Kopplungs-Muster von WebSocket trägt: dort schickt das Gerät mit der UI dem Ziel-Board dessen eigene Einstellungen per HTTP, was bei einem Broker keine Entsprechung hat. *(Opus · mittel · mit Plan)*

- **Energiemanagement-Einstellungsseite (Deep-Sleep-Betrieb)** (2026-09-22) — neue Unterseite in den Einstellungen: Pin für Batteriespannungsmessung (ADC), Mess-/Update-Intervall der Sensoren konfigurierbar, und darauf aufbauend Deep-Sleep zwischen Messungen aktivierbar. Dazu ein Pin, der signalisiert, ob das Gerät nur kurz aufgewacht ist (Messung + zurück in Deep-Sleep) oder vollständig wach (z. B. per Taster/Jumper) — im Kurz-Wach-Zustand sollen bestimmte Funktionen abschaltbar sein und nur im vollwachen Zustand zur Verfügung stehen. Offene Fragen: welche Subsysteme im Kurz-Wach-Zustand überhaupt aktiv sind (reine Messung + Funk-Publish z. B. per ESP-NOW, ohne Webserver?), wie das in den bestehenden `loop()`/Registry-Zyklus eingreift, und wie sich das mit Datalog/Zeitstempeln verträgt (ähnlicher RTC-Bedarf wie beim Hardware-RTC-Punkt, da NTP nach jedem Deep-Sleep-Wakeup zu teuer wäre). Zielbild klingt nach iSpindel-artigem Batteriebetrieb. *(Opus · groß · mit Plan — Architektur-Entscheidung, betrifft Firmware-Lebenszyklus)*

## Buckets (bei Gelegenheit)

- **Mobile Nutzung** (vorgemerkt 2026-08-12) — das Dashboard wird typischerweise am Kessel/Handy bedient, nicht am Desktop. Nach dem WinUI-Redesign prüfen: Touch-Ziele, Breakpoints, Lesbarkeit der Cards/NavShell auf kleinen Screens. *(Sonnet · mittel · mit Plan — Audit + mehrere Seiten systematisch durchgehen)*
